# Keep the application API internal

The planned HTTP endpoints form an internal backend-for-frontend contract for the Next.js application, not a public or partner API. The contract will cover implemented product use cases, the legal-corpus and Grounding Gateway additions, and clearly identified near-term domain gaps; external versioning, API keys, partner stability guarantees, and speculative endpoints remain out of scope.
