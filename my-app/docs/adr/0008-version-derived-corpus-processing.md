# Version derived corpus processing

A legal-source file version does not acquire silently replaceable extracted text, chunks, or embeddings. Each processing attempt produces an immutable generation identified by its parser, OCR, chunker, and embedding configuration; a Legal Corpus Release pins an approved source version and processing generation, and reprocessing can enter runtime use only through a later published release.
