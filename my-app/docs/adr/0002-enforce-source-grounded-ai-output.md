# Enforce source-grounded AI output

AI features will use grounded synthesis rather than treating model knowledge as a source or limiting output to copied extracts. Models receive no external search tools, material legal and organization-specific claims must cite supplied source IDs, citations are validated against the exact retrieved chunks, and insufficiently supported output must abstain or fail validation rather than becoming a persisted business result.
