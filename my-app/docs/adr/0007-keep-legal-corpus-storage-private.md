# Keep legal-corpus storage private

Original files for all Authority Tiers will live in a separate private `legal-corpus` Supabase Storage bucket. Users inspect source metadata and cited excerpts through authorized application endpoints, while access to an original file uses a short-lived signed URL; the uniform policy accommodates licensed secondary material without weakening controls for the whole corpus.
