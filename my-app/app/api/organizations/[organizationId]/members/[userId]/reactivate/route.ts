import { requireApiUser } from "@/src/server/api/auth"; import { requireIfMatch } from "@/src/server/api/concurrency"; import { apiRoute } from "@/src/server/api/handler"; import { setOrganizationMemberStatus } from "@/src/server/organizations/service";
export const POST = apiRoute(async ({ request, routeContext }: { request: Request; routeContext: { params: Promise<{ organizationId: string; userId: string }> } }) => { const actor = await requireApiUser(); const { organizationId, userId } = await routeContext.params; const member = await setOrganizationMemberStatus({ userId: actor.id, organizationId, memberUserId: userId, status: "active", expectedVersion: requireIfMatch(request) }); return { data: { member }, meta: { version: member.version } }; });
