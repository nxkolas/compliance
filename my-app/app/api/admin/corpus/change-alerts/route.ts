import { paginationQuerySchema } from "@/src/contracts/common/pagination"; import { requireApiUser } from "@/src/server/api/auth"; import { apiRoute } from "@/src/server/api/handler"; import { parseInput } from "@/src/server/api/request"; import { listChangeAlertsPage } from "@/src/server/corpus/admin-service";
export const GET = apiRoute(async ({ request }: { request: Request }) => { const user = await requireApiUser(); const query = parseInput(paginationQuerySchema, Object.fromEntries(new URL(request.url).searchParams)); const result = await listChangeAlertsPage({ userId: user.id, ...query }); return { data: { alerts: result.alerts }, meta: { nextCursor: result.nextCursor } }; });
