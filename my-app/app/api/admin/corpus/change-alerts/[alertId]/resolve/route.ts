import { changeAlertResolveSchema } from "@/src/contracts/admin"; import { requireApiUser } from "@/src/server/api/auth"; import { apiRoute } from "@/src/server/api/handler"; import { readJsonBody } from "@/src/server/api/request"; import { resolveLegalSourceChangeAlert } from "@/src/server/corpus/monitor-service"; import { requireIfMatch } from "@/src/server/api/concurrency";
export const POST = apiRoute(async ({ request, requestId, routeContext }: { request: Request; requestId: string; routeContext: { params: Promise<{ alertId: string }> } }) => { const user = await requireApiUser(); const { alertId } = await routeContext.params; const body = await readJsonBody(request, changeAlertResolveSchema); const result = await resolveLegalSourceChangeAlert({ actorUserId: user.id, alertId, requestId, expectedVersion: requireIfMatch(request), ...body }); return { data: result, meta: { version: result.alert.version } }; });
