import { corpusMonitorUpdateSchema } from "@/src/contracts/admin"; import { requireApiUser } from "@/src/server/api/auth"; import { requireIfMatch } from "@/src/server/api/concurrency"; import { apiRoute } from "@/src/server/api/handler"; import { readJsonBody } from "@/src/server/api/request"; import { updateCorpusMonitor } from "@/src/server/corpus";
export const PATCH = apiRoute(async ({ request, requestId, routeContext }: { request: Request; requestId: string; routeContext: { params: Promise<{ monitorId: string }> } }) => { const user = await requireApiUser(); const { monitorId } = await routeContext.params; const body = await readJsonBody(request, corpusMonitorUpdateSchema); const monitor = await updateCorpusMonitor({ actorUserId: user.id, monitorId, expectedVersion: requireIfMatch(request), requestId, ...body }); return { data: { monitor }, meta: { version: monitor.version } }; });
