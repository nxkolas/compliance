import { reasonSchema } from "@/src/contracts/admin"; import { requireApiUser } from "@/src/server/api/auth"; import { apiRoute } from "@/src/server/api/handler"; import { readJsonBody } from "@/src/server/api/request"; import { withdrawCorpusRelease } from "@/src/server/corpus/release-service";
export const POST = apiRoute(async ({ request, requestId, routeContext }: { request: Request; requestId: string; routeContext: { params: Promise<{ releaseId: string }> } }) => { const user = await requireApiUser(); const { releaseId } = await routeContext.params; const { reason } = await readJsonBody(request, reasonSchema); return { data: { release: await withdrawCorpusRelease({ actorUserId: user.id, releaseId, reason, requestId }) } }; });
