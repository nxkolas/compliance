import { reasonSchema } from "@/src/contracts/admin"; import { requireApiUser } from "@/src/server/api/auth"; import { requireIfMatch } from "@/src/server/api/concurrency"; import { apiRoute } from "@/src/server/api/handler"; import { readJsonBody } from "@/src/server/api/request"; import { withdrawLegalSource } from "@/src/server/corpus/catalog-service";
export const POST = apiRoute(async ({ request, requestId, routeContext }: { request: Request; requestId: string; routeContext: { params: Promise<{ sourceId: string }> } }) => { const user = await requireApiUser(); const { sourceId } = await routeContext.params; const { reason } = await readJsonBody(request, reasonSchema); const source = await withdrawLegalSource({ actorUserId: user.id, sourceId, reason, expectedVersion: requireIfMatch(request), requestId }); return { data: { source }, meta: { version: source.version } }; });
