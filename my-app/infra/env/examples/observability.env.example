# Copy these values into the protected host environment file. Do not commit it.
DASHBOARD_USERNAME=operations
DASHBOARD_PASSWORD=replace-with-random-dashboard-password
ALERTMANAGER_WEBHOOK_URL=https://alerts.example.com/replace
PROMETHEUS_RETENTION=30d
LOKI_RETENTION=720h
