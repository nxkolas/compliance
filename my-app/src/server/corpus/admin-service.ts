import { and, eq, gt, ilike, isNull, lt, or } from "drizzle-orm";
import * as z from "zod";
import { toLegalSourceVersionReadModel } from "./read-models";
export { toLegalSourceVersionReadModel } from "./read-models";
import { db } from "@/src/db";
import { legalCorpusFamilies, legalSourceMonitors, legalSources, platformAuditEvents } from "@/src/db/schema";
import { requirePlatformCapability } from "@/src/server/auth/capability-service";
import { ApiError } from "@/src/server/api/errors";
import { getCursorCodec } from "@/src/server/api/pagination";
import { legalSourceMonitorScheduleSchema } from "@/src/contracts/admin";
import { syncLegalSourceMonitorJob } from "./monitor-scheduler";

const dateCursorSchema = z.tuple([z.iso.datetime(), z.uuid()]);
const textCursorSchema = z.tuple([z.string(), z.uuid()]);

export async function getCorpusFamily(userId: string, familyId: string) { await requirePlatformCapability(userId, "corpus:read"); const row = await db.query.legalCorpusFamilies.findFirst({ columns: { id: true, code: true, frameworkCode: true, jurisdictionCode: true, title: true, archivedAt: true, version: true, createdBy: true, createdAt: true, updatedAt: true }, where: { RAW: (table, operators) => (eq(table.id, familyId)) ?? operators.sql`true` } }); if (!row) throw new ApiError(404, "Corpus family not found", undefined, "CORPUS_FAMILY_NOT_FOUND"); return row; }
export async function updateCorpusFamily(input: { actorUserId: string; familyId: string; title: string; archived?: boolean; expectedVersion: number; requestId?: string }) { await requirePlatformCapability(input.actorUserId, "corpus:curate"); return db.transaction(async (tx) => { const [row] = await tx.update(legalCorpusFamilies).set({ title: input.title, archivedAt: input.archived === undefined ? undefined : input.archived ? new Date() : null, version: input.expectedVersion + 1, updatedAt: new Date() }).where(and(eq(legalCorpusFamilies.id, input.familyId), eq(legalCorpusFamilies.version, input.expectedVersion))).returning(); if (!row) throw new ApiError(412, "Corpus family changed", undefined, "PRECONDITION_FAILED"); await tx.insert(platformAuditEvents).values({ actorUserId: input.actorUserId, eventType: "legal_corpus_family.updated", entityType: "legal_corpus_family", entityId: row.id, requestId: input.requestId, metadata: { archived: Boolean(row.archivedAt), version: row.version } }); return row; }); }
export async function listLegalSources(userId: string, query?: string) { return (await listLegalSourcesPage({ userId, query, limit: 100 })).sources; }
export async function listLegalSourcesPage(input: { userId: string; query?: string; limit: number; cursor?: string }) { await requirePlatformCapability(input.userId, "corpus:read"); const normalizedQuery = input.query?.trim() || undefined; const scope = `corpus-sources:${normalizedQuery ?? ""}`; const cursor = input.cursor ? textCursorSchema.parse(getCursorCodec().decode(input.cursor, scope)) : null; const rows = await db.query.legalSources.findMany({ columns: { id: true, familyId: true, stableCode: true, title: true, sourceKind: true, authorityTier: true, canonicalPublisher: true, legalInstrumentId: true, legalProvisionId: true, withdrawnAt: true, withdrawalReason: true, version: true, createdBy: true, createdAt: true, updatedAt: true }, where: { RAW: (table, operators) => (and(normalizedQuery ? ilike(table.title, `%${normalizedQuery}%`) : undefined, cursor ? or(gt(table.title, cursor[0]), and(eq(table.title, cursor[0]), gt(table.id, cursor[1]))) : undefined)) ?? operators.sql`true` }, orderBy: { title: "asc", id: "asc" }, limit: input.limit + 1 }); const page = rows.slice(0, input.limit); const last = page.at(-1); return { sources: page, nextCursor: rows.length > input.limit && last ? getCursorCodec().encode(scope, [last.title, last.id]) : undefined }; }
export async function getLegalSource(userId: string, sourceId: string) { await requirePlatformCapability(userId, "corpus:read"); const source = await db.query.legalSources.findFirst({ columns: { id: true, familyId: true, stableCode: true, title: true, sourceKind: true, authorityTier: true, canonicalPublisher: true, legalInstrumentId: true, legalProvisionId: true, withdrawnAt: true, withdrawalReason: true, version: true, createdBy: true, createdAt: true, updatedAt: true }, where: { RAW: (table, operators) => (eq(table.id, sourceId)) ?? operators.sql`true` } }); if (!source) throw new ApiError(404, "Legal source not found", undefined, "LEGAL_SOURCE_NOT_FOUND"); const versions = await db.query.legalSourceVersions.findMany({ columns: { id: true, sourceId: true, versionLabel: true, officialIdentifier: true, upstreamPublishedAt: true, retrievedAt: true, upstreamUrl: true, effectiveFrom: true, effectiveTo: true, contentHash: true, status: true, reviewedBy: true, reviewedAt: true, publishedAt: true, withdrawnBy: true, withdrawnAt: true, withdrawalReason: true, createdBy: true, createdAt: true }, where: { RAW: (table, operators) => (eq(table.sourceId, source.id)) ?? operators.sql`true` }, orderBy: { createdAt: "desc" } }); return { source, versions: versions.map(toLegalSourceVersionReadModel) }; }
export async function updateLegalSource(input: { actorUserId: string; sourceId: string; title: string; canonicalPublisher: string; expectedVersion: number; requestId?: string }) { await requirePlatformCapability(input.actorUserId, "corpus:curate"); return db.transaction(async (tx) => { const [source] = await tx.update(legalSources).set({ title: input.title, canonicalPublisher: input.canonicalPublisher, version: input.expectedVersion + 1, updatedAt: new Date() }).where(and(eq(legalSources.id, input.sourceId), eq(legalSources.version, input.expectedVersion), isNull(legalSources.withdrawnAt))).returning(); if (!source) throw new ApiError(412, "Legal source changed", undefined, "PRECONDITION_FAILED"); await tx.insert(platformAuditEvents).values({ actorUserId: input.actorUserId, eventType: "legal_source.updated", entityType: "legal_source", entityId: source.id, requestId: input.requestId, metadata: { version: source.version } }); return source; }); }
export async function getLegalSourceVersion(userId: string, versionId: string) { await requirePlatformCapability(userId, "corpus:read"); const version = await db.query.legalSourceVersions.findFirst({ columns: { id: true, sourceId: true, versionLabel: true, officialIdentifier: true, upstreamPublishedAt: true, retrievedAt: true, upstreamUrl: true, effectiveFrom: true, effectiveTo: true, contentHash: true, status: true, reviewedBy: true, reviewedAt: true, publishedAt: true, withdrawnBy: true, withdrawnAt: true, withdrawalReason: true, createdBy: true, createdAt: true }, where: { RAW: (table, operators) => (eq(table.id, versionId)) ?? operators.sql`true` } }); if (!version) throw new ApiError(404, "Source version not found", undefined, "LEGAL_SOURCE_VERSION_NOT_FOUND"); const renditions = await db.query.legalSourceRenditions.findMany({ columns: { id: true, sourceVersionId: true, language: true, translationStatus: true, authoritativeRenditionId: true, storageBucket: true, storagePath: true, mimeType: true, byteSize: true, contentHash: true, duplicateAcknowledged: true, uploadSessionId: true, importJobId: true, importedFromUrl: true, createdBy: true, createdAt: true }, where: { RAW: (table, operators) => (eq(table.sourceVersionId, version.id)) ?? operators.sql`true` } }); const generations = renditions.length ? await db.query.legalSourceProcessingGenerations.findMany({ columns: { id: true, renditionId: true, jobId: true, embeddingJobId: true, generationNumber: true, state: true, parserConfig: true, ocrConfig: true, chunkerConfig: true, embeddingConfig: true, extractionHash: true, normalizedTextHash: true, qualityMetrics: true, reliableAnchors: true, reviewerId: true, reviewedAt: true, safeErrorCode: true, createdAt: true, updatedAt: true }, where: { RAW: (row, { inArray }) => inArray(row.renditionId, renditions.map((item) => item.id)) } }) : []; return { version: toLegalSourceVersionReadModel(version), renditions, generations }; }
export async function getLegalChunk(userId: string, chunkId: string) { await requirePlatformCapability(userId, "corpus:read"); const chunk = await db.query.legalSourceChunks.findFirst({ columns: { id: true, generationId: true, position: true, text: true, textHash: true, pageNumber: true, sectionPath: true, provisionCode: true, anchorMetadata: true, tokenCount: true, searchVector: true, createdAt: true }, where: { RAW: (table, operators) => (eq(table.id, chunkId)) ?? operators.sql`true` } }); if (!chunk) throw new ApiError(404, "Chunk not found", undefined, "LEGAL_CHUNK_NOT_FOUND"); return chunk; }
export async function listCorpusReleases(userId: string) { return (await listCorpusReleasesPage({ userId, limit: 100 })).releases; }
export async function listCorpusReleasesPage(input: { userId: string; limit: number; cursor?: string }) { await requirePlatformCapability(input.userId, "corpus:read"); const scope = "corpus-releases"; const cursor = input.cursor ? dateCursorSchema.parse(getCursorCodec().decode(input.cursor, scope)) : null; const rows = await db.query.legalCorpusReleases.findMany({ columns: { id: true, familyId: true, versionLabel: true, contentHash: true, status: true, evaluationState: true, evaluationJobId: true, publishedBy: true, publishedAt: true, withdrawnBy: true, withdrawnAt: true, withdrawalReason: true, version: true, createdBy: true, createdAt: true, updatedAt: true }, where: { RAW: (table, operators) => (cursor ? or(lt(table.createdAt, new Date(cursor[0])), and(eq(table.createdAt, new Date(cursor[0])), lt(table.id, cursor[1]))) : undefined) ?? operators.sql`true` }, orderBy: { createdAt: "desc", id: "desc" }, limit: input.limit + 1 }); const page = rows.slice(0, input.limit); const last = page.at(-1); return { releases: page, nextCursor: rows.length > input.limit && last ? getCursorCodec().encode(scope, [last.createdAt.toISOString(), last.id]) : undefined }; }
export async function getCorpusRelease(userId: string, releaseId: string) { await requirePlatformCapability(userId, "corpus:read"); const release = await db.query.legalCorpusReleases.findFirst({ columns: { id: true, familyId: true, versionLabel: true, contentHash: true, status: true, evaluationState: true, evaluationJobId: true, publishedBy: true, publishedAt: true, withdrawnBy: true, withdrawnAt: true, withdrawalReason: true, version: true, createdBy: true, createdAt: true, updatedAt: true }, where: { RAW: (table, operators) => (eq(table.id, releaseId)) ?? operators.sql`true` } }); if (!release) throw new ApiError(404, "Corpus release not found", undefined, "CORPUS_RELEASE_NOT_FOUND"); return { release, members: await db.query.legalCorpusReleaseMembers.findMany({ columns: { releaseId: true, sourceVersionId: true, renditionId: true, processingGenerationId: true, position: true }, where: { RAW: (table, operators) => (eq(table.releaseId, release.id)) ?? operators.sql`true` }, orderBy: { position: "asc" } }) }; }
export async function listCorpusMonitors(userId: string) { return (await listCorpusMonitorsPage({ userId, limit: 100 })).monitors; }
export async function listCorpusMonitorsPage(input: { userId: string; limit: number; cursor?: string }) { await requirePlatformCapability(input.userId, "corpus:read"); const scope = "corpus-monitors"; const cursor = input.cursor ? dateCursorSchema.parse(getCursorCodec().decode(input.cursor, scope)) : null; const rows = await db.query.legalSourceMonitors.findMany({ columns: { id: true, sourceId: true, exactUrl: true, schedule: true, active: true, etag: true, lastModified: true, lastCheckedAt: true, nextCheckAt: true, version: true, createdBy: true, createdAt: true, updatedAt: true }, where: { RAW: (table, operators) => (cursor ? or(gt(table.nextCheckAt, new Date(cursor[0])), and(eq(table.nextCheckAt, new Date(cursor[0])), gt(table.id, cursor[1]))) : undefined) ?? operators.sql`true` }, orderBy: { nextCheckAt: "asc", id: "asc" }, limit: input.limit + 1 }); const page = rows.slice(0, input.limit); const last = page.at(-1); return { monitors: page, nextCursor: rows.length > input.limit && last ? getCursorCodec().encode(scope, [last.nextCheckAt.toISOString(), last.id]) : undefined }; }
export async function updateCorpusMonitor(input: { actorUserId: string; monitorId: string; schedule: string; paused: boolean; expectedVersion: number; requestId?: string }) {
  await requirePlatformCapability(input.actorUserId, "corpus:operate");
  const schedule = legalSourceMonitorScheduleSchema.parse(input.schedule);
  return db.transaction(async (tx) => {
    const current = await tx.query.legalSourceMonitors.findFirst({ columns: { id: true, sourceId: true, exactUrl: true, schedule: true, active: true, etag: true, lastModified: true, lastCheckedAt: true, nextCheckAt: true, version: true, createdBy: true, createdAt: true, updatedAt: true },
      where: { RAW: (table, operators) => (and(
        eq(table.id, input.monitorId),
        eq(table.version, input.expectedVersion),
      )) ?? operators.sql`true` },
    });
    if (!current) throw new ApiError(412, "Monitor changed", undefined, "PRECONDITION_FAILED");
    const active = !input.paused;
    const nextCheckAt = active && (!current.active || current.schedule !== schedule)
      ? new Date()
      : current.nextCheckAt;
    const [monitor] = await tx.update(legalSourceMonitors).set({
      schedule,
      active,
      nextCheckAt,
      version: input.expectedVersion + 1,
      updatedAt: new Date(),
    }).where(and(
      eq(legalSourceMonitors.id, input.monitorId),
      eq(legalSourceMonitors.version, input.expectedVersion),
    )).returning();
    if (!monitor) throw new ApiError(412, "Monitor changed", undefined, "PRECONDITION_FAILED");
    await syncLegalSourceMonitorJob(tx, {
      monitorId: monitor.id,
      active: monitor.active,
      runAfter: monitor.nextCheckAt,
      requestedByUserId: input.actorUserId,
    });
    await tx.insert(platformAuditEvents).values({ actorUserId: input.actorUserId, eventType: "legal_source_monitor.updated", entityType: "legal_source_monitor", entityId: monitor.id, requestId: input.requestId, metadata: { active: monitor.active, schedule: monitor.schedule, nextCheckAt: monitor.nextCheckAt.toISOString(), version: monitor.version } });
    return monitor;
  });
}
export async function listChangeAlerts(userId: string) { return (await listChangeAlertsPage({ userId, limit: 100 })).alerts; }
export async function listChangeAlertsPage(input: { userId: string; limit: number; cursor?: string }) { await requirePlatformCapability(input.userId, "corpus:read"); const scope = "corpus-change-alerts"; const cursor = input.cursor ? dateCursorSchema.parse(getCursorCodec().decode(input.cursor, scope)) : null; const rows = await db.query.legalSourceChangeAlerts.findMany({ columns: { id: true, monitorCheckId: true, sourceId: true, oldHash: true, newHash: true, candidateVersionId: true, state: true, resolvedBy: true, resolutionReason: true, resolvedAt: true, version: true, createdAt: true }, where: { RAW: (table, operators) => (cursor ? or(lt(table.createdAt, new Date(cursor[0])), and(eq(table.createdAt, new Date(cursor[0])), lt(table.id, cursor[1]))) : undefined) ?? operators.sql`true` }, orderBy: { createdAt: "desc", id: "desc" }, limit: input.limit + 1 }); const page = rows.slice(0, input.limit); const last = page.at(-1); return { alerts: page, nextCursor: rows.length > input.limit && last ? getCursorCodec().encode(scope, [last.createdAt.toISOString(), last.id]) : undefined }; }
